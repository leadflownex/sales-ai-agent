import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'LeadFlowNex - AI Powered Sales Automation',
  description: 'Transform your sales process with AI-powered lead generation, multi-channel outreach, and advanced analytics.',
  keywords: 'sales automation, lead generation, AI sales, marketing automation',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gray-50">
          {children}
        </div>
      </body>
    </html>
  )
}
